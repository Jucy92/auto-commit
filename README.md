# 🌱 Auto Commit Tracker

GitHub 잔디(커밋 기록)를 자동으로 관리하는 시스템입니다.

매일 오후 11시 50분에 자동으로 실행되어, 오늘 커밋이 없으면 자동으로 커밋을 생성합니다.

## ✨ 주요 기능

- **자동 커밋 체크**: 매일 23:50에 GitHub API로 오늘 커밋 확인
- **자동 커밋 생성**: 커밋이 없으면 `auto commit Nday` 형태로 자동 커밋
- **카운터 관리**: 연속 무커밋 일수를 자동으로 추적 (1day, 2day, 3day...)
- **자동 리셋**: 사용자가 직접 커밋하면 카운터가 0으로 초기화
- **완전 자동화**: 한 번 설정하면 아무것도 할 필요 없음!

## 📂 프로젝트 구조

```
auto-commit-tracker/
├── .github/
│   └── workflows/
│       └── auto-commit.yml        # GitHub Actions 워크플로우
├── src/
│   └── check-and-commit.js        # 메인 로직
├── logs/
│   └── commit-log.md              # 커밋 이력 로그
├── counter.txt                     # 카운터 (현재 연속 일수)
├── package.json
└── README.md
```

## 🚀 시작하기

### 1. 저장소 클론

```bash
git clone https://github.com/Jucy92/auto-commit.git
cd auto-commit
```

### 2. 필요한 설정

#### GitHub Repository Settings

1. **Settings** → **Actions** → **General** 이동
2. **Workflow permissions** 섹션에서:
   - ✅ `Read and write permissions` 선택
   - ✅ `Allow GitHub Actions to create and approve pull requests` 체크

#### 환경 변수 (선택사항)

다른 사용자를 대상으로 하려면 `.github/workflows/auto-commit.yml` 파일의 `TARGET_USER` 변경:

```yaml
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  TARGET_USER: 다른사용자이름  # 여기 수정
```

### 3. 첫 푸시

```bash
git add .
git commit -m "Initial setup"
git push origin main
```

### 4. 완료!

이제 매일 23:50 (한국 시간)에 자동으로 실행됩니다.

## 🔧 작동 방식

### 시나리오 1: 연속 무커밋

```
Day 1 (23:50) → 커밋 없음 → "auto commit 1day" 생성
Day 2 (23:50) → 커밋 없음 → "auto commit 2day" 생성
Day 3 (23:50) → 커밋 없음 → "auto commit 3day" 생성
```

### 시나리오 2: 사용자 커밋 후 리셋

```
Day 1 (23:50) → 커밋 없음 → "auto commit 1day" 생성
Day 2 (23:50) → 커밋 없음 → "auto commit 2day" 생성
Day 3 (15:00) → 사용자가 직접 커밋!
Day 3 (23:50) → 커밋 있음 → 카운터 리셋 (2 → 0)
Day 4 (23:50) → 커밋 없음 → "auto commit 1day" 생성 (다시 1부터!)
```

### 시나리오 3: 정상 활동

```
Day 1 (10:00) → 사용자가 직접 커밋
Day 1 (23:50) → 커밋 있음 → 아무것도 안 함
Day 2 (14:00) → 사용자가 직접 커밋
Day 2 (23:50) → 커밋 있음 → 아무것도 안 함
```

## 🧪 테스트하기

### 수동으로 실행

1. GitHub 저장소 페이지로 이동
2. **Actions** 탭 클릭
3. 왼쪽에서 **Daily Auto Commit Check** 선택
4. **Run workflow** 버튼 클릭

### 로컬에서 테스트

```bash
# 의존성 설치
npm install

# 스크립트 실행 (GITHUB_TOKEN 필요)
export GITHUB_TOKEN=your_github_token_here
export TARGET_USER=Jucy92
npm start
```

## 📊 로그 확인

자동 커밋 이력은 `logs/commit-log.md` 파일에서 확인할 수 있습니다:

```markdown
# Auto Commit Log

- 2025-11-24: auto commit 1day
- 2025-11-25: auto commit 2day
- 2025-11-26: Manual commit detected. Counter reset from 2 to 0.
- 2025-11-27: auto commit 1day
```

## 🔍 GitHub Actions 로그 확인

1. GitHub 저장소 → **Actions** 탭
2. 최근 실행 기록 클릭
3. 상세 로그 확인

## ⚠️ 주의사항

### 1. 시간대

- GitHub Actions는 **UTC 시간**으로 작동
- 한국 시간 23:50 = UTC 14:50
- `cron: '50 14 * * *'`로 설정됨

### 2. 자동 커밋 제외

- 메시지에 "auto commit"이 포함된 커밋은 자동으로 제외됨
- 수동 커밋만 카운터 리셋 조건으로 인식

### 3. 권한 설정 필수

```
Settings → Actions → General
→ Workflow permissions
→ Read and write permissions 선택
```

## 🛠️ 문제 해결

### 커밋이 안 돼요!

1. **권한 확인**: `Settings → Actions → Workflow permissions`에서 Write 권한 확인
2. **Actions 탭 확인**: 에러 로그 확인
3. **수동 실행**: `Actions` 탭에서 수동으로 워크플로우 실행해보기

### 카운터가 리셋이 안 돼요!

- 다른 저장소에 커밋해도 인식됩니다 (GitHub API는 모든 public 저장소 커밋 확인)
- Private 저장소 커밋은 인식 안 될 수 있음

### 시간이 안 맞아요!

- GitHub Actions의 cron은 UTC 기준
- 한국 시간 23:50 = UTC 14:50
- 필요시 `.github/workflows/auto-commit.yml`의 cron 수정

## 📝 라이선스

MIT License

## 👤 작성자

**Jucy92**

- GitHub: [@Jucy92](https://github.com/Jucy92)

## 🙏 기여

이슈나 PR은 언제든 환영합니다!

---

**Made with ❤️ by GitHub Actions**
